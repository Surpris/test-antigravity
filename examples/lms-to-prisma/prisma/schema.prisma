// This file is automatically generated. Do not edit directly.
// Source: logical_model.yaml

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
}

enum ContributorRoleInProject {
  Principal_Investigator @map("Principal Investigator")
  Co_Investigator @map("Co-Investigator")
  Data_Collector @map("Data Collector")
  Data_Manager @map("Data Manager")
}

enum DatasetAccessPolicy {
  Option_1 @map("公開")
  Option_2 @map("共有")
  Option_3 @map("非共有・非公開")
}

model DMPMetadata {
  id String @id @default(uuid())
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
  deletedAt DateTime?
  createdDate DateTime @db.Date
  lastUpdatedDate DateTime @db.Date
}

model Project {
  id String @id @default(uuid())
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
  deletedAt DateTime?
  projectNumber String @unique
  hasContributors Contributor[] @relation("HasContributors")
  hasDatasets Dataset[] @relation("HasDatasets")
}

model Contributor {
  id String @id @default(uuid())
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
  deletedAt DateTime?
  contributorId String @unique
  name String
  roleInProject ContributorRoleInProject?
  hasContributorsProjectId String
  hasContributorsProject Project @relation("HasContributors", fields: [hasContributorsProjectId], references: [id])
  managedByDatasets Dataset[] @relation("ManagedBy")
}

model Dataset {
  id String @id @default(uuid())
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
  deletedAt DateTime?
  datasetNo Int @unique
  title String
  accessPolicy DatasetAccessPolicy?
  managedFrom DateTime? @db.Date
  managedTo DateTime? @db.Date
  hasDatasetsProjectId String
  hasDatasetsProject Project @relation("HasDatasets", fields: [hasDatasetsProjectId], references: [id])
  managedById String?
  managedBy Contributor? @relation("ManagedBy", fields: [managedById], references: [id])
}


// --- User Defined System Models ---
model UserDefinedRelationship {
  id        String   @id @default(uuid())
  sourceId  String
  targetId  String
  type      String
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
  
  @@index([sourceId])
  @@index([targetId])
}
